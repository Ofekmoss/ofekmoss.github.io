export class Team {
    constructor(
        public name: string, 
        public league: string, 
        public rank: number, 
        public image_path: string, 
        public type: string) {
    }
}